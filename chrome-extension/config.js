/**
 * South Cinema Insights — Environment Config
 *
 * LOCAL:      http://localhost:8000
 * PRODUCTION: https://api.southcinema.app  ← update this when deploying
 *
 * Also update host_permissions in manifest.json to match.
 */
const SCA_CONFIG = {
  API_BASE: 'http://localhost:8000',
};
